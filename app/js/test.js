/*var age = 18,
  firstname = "Сергей",
  follow = true;
function accses( name, age, follow ) {
  if ( age >= 18 && follow == true ) {
    alert("Доступ разрешен, "+ name);
    }
  else {
	alert("Доступ запрещен");
    }
 }  
accses(firstname,age,follow);
*/